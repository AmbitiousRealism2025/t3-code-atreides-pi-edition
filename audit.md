# Source File Audit — Largest Files by Line Count

Generated: 2026-03-09

| # | File | Lines | Summary |
|---|------|------:|---------|
| 1 | `apps/web/src/components/ChatView.tsx` | 6,124 | The main chat UI component for the web app. It renders the full conversation view including the message timeline (with virtualized scrolling), the composer input area with image attachments and slash commands, provider/model selection controls, approval and user-input prompts, a plan sidebar, turn-level diff summaries, branch/worktree management, and thread terminal drawers. It wires together most of the client-side state (Zustand store, TanStack Query, composer draft store, terminal state) and handles the bulk of user interaction logic for sending turns, retrying messages, managing project scripts, and keyboard shortcuts. This is effectively the "god component" of the frontend. |
| 2 | `apps/server/src/orchestration/Layers/ProjectionPipeline.test.ts` | 1,936 | The test suite for the orchestration projection pipeline on the server. It exercises the full event-sourcing read-model projection flow: appending domain events (project created, thread created, turn started/completed, messages added, checkpoints, approval requests, etc.) to the event store, running them through the projection pipeline, and asserting that the resulting SQL read-model rows (projects, threads, turns, messages, activities, pending approvals) are correct. It covers edge cases like idempotent re-projection, out-of-order event handling, thread-scoped projections, multi-project isolation, and checkpoint/diff metadata propagation. |
| 3 | `apps/server/src/wsServer.test.ts` | 1,800 | The integration test suite for the WebSocket server (`wsServer.ts`). It spins up a real HTTP + WebSocket server with mocked service layers (terminal manager, provider service, git core, analytics) and validates the full RPC surface: welcome handshake, auth token gating, static file serving (including persisted attachments and path-traversal rejection), server config methods, keybinding bootstrapping and upsert, terminal RPC routing and event broadcasting, orchestration domain event push for provider runtime events, project CRUD error handling, `projects.searchEntries`, `projects.writeFile` with workspace-root sandboxing, git core method routing, turn diff queries, and graceful handling of malformed JSON and handler exceptions. |
